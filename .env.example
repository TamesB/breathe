# Supabase project credentials (Project Settings -> API).
# Copy this file to .env.local and fill in your values.
# Leave these unset to run the app fully in guest mode (localStorage only).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
